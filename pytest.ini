[pytest]
addopts = -v -s --tb=short --strict-markers  --alluredir=allure-results --clean-alluredir
testpaths = tests
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s - %(levelname)s - %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S
markers =
    event_search: 信号ID搜索测试